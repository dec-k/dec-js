import React from 'react';
import Aux from '../../hoc/Auxiliary/auxiliary'

const header = (props) => {
    return (
        <Aux>
            <h1>I'm a header</h1>
        </Aux>
    );
}

export default header;